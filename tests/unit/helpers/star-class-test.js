import { starClass } from 'lesson-02/helpers/star-class';
import { module, test } from 'qunit';

module('Unit | Helper | star class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = starClass([42]);
  assert.ok(result);
});
